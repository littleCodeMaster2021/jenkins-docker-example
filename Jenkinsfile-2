stage('Initialize'){
        def dockerHome = tool 'myDocker'
        env.PATH = "${dockerHome}/bin:${env.PATH}"
    }
    
pipeline {
    agent {
        docker { image 'jenkins/jenkins:lts' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'ls'
            }
        }
    }
}
